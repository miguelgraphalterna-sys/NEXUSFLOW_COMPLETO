# NexusFlow - Plataforma Omnicanal CRM
Gestiona WhatsApp, Facebook e Instagram en un dashboard.

## Inicio Rápido
Backend: cd backend && npm install && npm run dev
Frontend: cd frontend && npm install && npm run dev

Frontend: http://localhost:5173 | Backend: http://localhost:3000

Configuración: EST timezone, USD, $0 pipeline inicial